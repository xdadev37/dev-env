name: dev-env

services:
  node:
    image: node
    entrypoint: /usr/bin/tail -f /dev/null
    volumes:
      - node:/usr/local/bin
      - yarn:/opt/yarn-v1.22.19
    extends:
      service: common
      file: ./common.yaml

  dotnet:
    image: mcr.microsoft.com/dotnet/sdk
    entrypoint: /usr/bin/tail -f /dev/null
    volumes:
      - dotnet:/usr/share/dotnet
    extends:
      service: common
      file: ./common.yaml

  powershell:
    image: mcr.microsoft.com/powershell
    entrypoint: /usr/bin/tail -f /dev/null
    volumes:
      - pwsh:/opt/microsoft/powershell/7
      - libicu:/usr/lib/x86_64-linux-gnu
    extends:
      service: common
      file: ./common.yaml

  docker:
    image: docker
    privileged: true
    volumes:
      - docker:/usr/local/bin
    extends:
      service: common
      file: ./common.yaml

  vscode:
    image: gitpod/openvscode-server
    environment:
      PATH: /usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/var/dotnet:/var/node:/var/pwsh:/var/docker:/var/yarn/bin
    volumes:
      - vscode:/home/workspace
      - /home/${USERNAME}/.ssh:/home/openvscode-server/.ssh
      - /home/${USERNAME}:/home/${USERNAME}
      - /var/run/docker.sock:/var/run/docker.sock
      - dotnet:/var/dotnet
      - docker:/var/docker
      - node:/var/node
      - pwsh:/var/pwsh
      - yarn:/var/yarn
      - libicu:/usr/lib/x86_64-linux-gnu
    extends:
      service: common
      file: ./common.yaml

volumes:
  vscode:
    name: vscode
  node:
    name: node
  dotnet:
    name: dotnet
  pwsh:
    name: pwsh
  docker:
    name: docker
  libicu:
    name: libicu
  yarn:
    name: yarn
